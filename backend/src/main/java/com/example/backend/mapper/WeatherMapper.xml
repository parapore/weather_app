<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.mapper.WeatherMapper">
    <select id="findAll" resultType="com.example.backend.entity.Weather">
        SELECT * FROM test.weather
        order by prefecture, yyyy_mm;
    </select>
</mapper>
